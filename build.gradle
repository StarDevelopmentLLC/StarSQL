plugins {
    id 'java-library'
    id 'maven-publish'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        url = 'https://www.jitpack.io'
    }
}

publishing {
    publications {
        shadow(MavenPublication) { publication ->
            project.shadow.component(publication)
        }
    }
}

group 'com.github.StarDevelopmentLLC'
version '1.0.0-alpha.13'

shadowJar {
    exclude('com.google.protobuf')
    dependencies {
        exclude(dependency('com.github.StarDevelopmentLLC.StarLib:observable-base:1.0.0-alpha.20'))
        exclude(dependency('com.github.StarDevelopmentLLC.StarLib:reflection:1.0.0-alpha.20'))
        exclude(dependency('com.github.StarDevelopmentLLC.StarLib:registry:1.0.0-alpha.20'))
        exclude(dependency('com.github.StarDevelopmentLLC.StarLib:misc:1.0.0-alpha.20'))
        exclude(dependency('com.github.StarDevelopmentLLC.StarLib:helper:1.0.0-alpha.20'))
    }
    archiveClassifier.set("")
    //destinationDirectory = file("${project.rootDir}" + File.separator + "build")
}

dependencies {
    implementation 'com.github.StarDevelopmentLLC.StarLib:observable-base:1.0.0-alpha.20'
    implementation 'com.github.StarDevelopmentLLC.StarLib:reflection:1.0.0-alpha.20'
    implementation 'com.github.StarDevelopmentLLC.StarLib:registry:1.0.0-alpha.20'
    implementation 'com.github.StarDevelopmentLLC.StarLib:misc:1.0.0-alpha.20'
    implementation 'com.github.StarDevelopmentLLC.StarLib:helper:1.0.0-alpha.20'
    api('com.mysql:mysql-connector-j:8.1.0') {
        exclude group: 'com.google.protobuf', module: 'protobuf-java'
    }
}

java.toolchain.languageVersion = JavaLanguageVersion.of(17)
tasks.jar.finalizedBy(shadowJar)
tasks.publishToMavenLocal.dependsOn(shadowJar)
tasks.compileJava.dependsOn(clean)
tasks.build.finalizedBy(publishToMavenLocal)